The schema behind `POST /api/waitlist`, kept here so the database is reviewable
in the repo rather than only in the dashboard. Both migrations are already
applied to the **PIN UI** project (`jaiclhpjldjewzlrxkcp`); these files are the
record, and what you would replay against a fresh project.
